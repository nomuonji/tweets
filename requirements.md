# SNS運用アプリ 要件

## プロダクトの役割

外部で反応の強い投稿パターンを見つけ、アカウントのテーマに翻訳し、生成・承認・自動投稿・結果確認を一つのループで回す。

## 中核ループ

1. アカウントごとのテーマと探索キーワードを設定する
2. Xの検索結果と承認済み参考アカウントから候補を収集する
3. 候補を本文ではなく、フック・構造・反応理由のパターンに変換する
4. パターン、自分の人格、投稿済み内容を使って投稿案を生成する
5. 下書きを承認して予約投稿する
6. 同期した結果を、次のパターン評価に戻す

## 非目標

- 全SNSの全投稿を網羅的に収集すること
- 外部投稿の本文をコピーして投稿すること
- AI生成結果を確認なしで即時公開すること
- プロンプトや生JSONを日常ユーザーに編集させること

## 主導線

- `/dashboard`: 今日の発見、生成案、投稿キュー、直近結果
- `/accounts`: 接続、テーマ、探索キーワード、参考アカウント、自動投稿設定
- `/analytics`: 投稿結果と、採用したパターンの成績

## 主導線から外すもの

- `/admin/simulation`: 開発者向け。通常ナビゲーションから除外
- `/tips`: 個別Tips CRUD。参考アカウントとパターンに統合
- `/schedule`: ダッシュボードの投稿キューに統合
- `/ranking`: `/analytics`へ統合

## 移行方針

- 既存の下書き、予約、投稿、OAuth、スケジューラーのデータ形式は維持する
- 新しい生成は外部パターンを必須の入力にする
- 新仕様で生成された下書きも既存の `drafts` と同じキューへ保存する
- 自動投稿の実行器は生成方式を意識せず、承認済み下書きだけを投稿する

## API制約

- 外部検索はキーワードごとに一日一回を基本とする
- 取得結果はFirestoreに保存し、生成のたびに外部APIを呼ばない
- 取得対象はユーザーが設定したキーワードと承認済みアカウントに限定する
- Threadsの外部探索は後回しにし、Xでループを成立させる

## 完成条件

- 主ナビゲーションが「ダッシュボード」「アカウント」「分析」の3本になる
- 参考アカウントを登録し、外部投稿を保存できる
- 外部投稿から抽出したパターンが生成プロンプトに含まれる
- 新仕様で生成した下書きが既存スケジューラーで投稿できる
- 既存の自動投稿を停止せず、アカウント単位で移行できる
- `npm run build` が成功する

## 現在地

- [x] 現状の生成プロンプトに既存データを接続
- [x] 外部参考アカウントと探索キーワードのデータモデル
- [x] 外部投稿の収集とキャッシュ
- [x] パターン抽出と新仕様生成
- [x] 主導線の再構成
- [ ] 既存スケジューラーとの接続確認

# SNS運用アプリ 要件

## プロダクトの役割

外部で反応の強い投稿パターンを見つけ、アカウントのテーマに翻訳し、生成・承認・自動投稿・結果確認を一つのループで回す。

## 中核ループ

1. アカウントごとのテーマと探索キーワードを設定する
2. Xの検索結果と承認済み参考アカウントから候補を収集する
3. 候補を本文ではなく、フック・構造・反応理由のパターンに変換する
4. パターン、自分の人格、投稿済み内容を使って投稿案を生成する
5. 下書きを承認して予約投稿する
6. 同期した結果を、次のパターン評価に戻す
7. 型別の反応率を週次で集計し、次の生成で強い型を優先・弱い型を抑制する
8. 週次のLLM分析で勝ち内容（話題・特性）と避ける内容を抽出し、生成に反映する
9. 探索率（既定0.2）で実績の型とは別の新構造・新話題を試し、過去の踏襲に収束させない
10. 探索キーワード未設定・自動生成が古いアカウントには、週次の分析でキーワードを自動生成する

## 非目標

- 全SNSの全投稿を網羅的に収集すること
- 外部投稿の本文をコピーして投稿すること
- AI生成結果を確認なしで即時公開すること
- プロンプトや生JSONを日常ユーザーに編集させること

## 主導線

- `/dashboard`: 今日の発見、生成案、投稿キュー、直近結果
- `/accounts`: 接続、テーマ、探索キーワード、参考アカウント、自動投稿設定
- `/analytics`: 投稿結果と、採用したパターンの成績

## 主導線から外すもの

- `/admin/simulation`: 開発者向け。通常ナビゲーションから除外
- `/tips`: 個別Tips CRUD。参考アカウントとパターンに統合
- `/schedule`: ダッシュボードの投稿キューに統合
- `/ranking`: `/analytics`へ統合

## 移行方針

- 既存の下書き、予約、投稿、OAuth、スケジューラーのデータ形式は維持する
- 新しい生成は外部パターンを必須の入力にする
- 新仕様で生成された下書きも既存の `drafts` と同じキューへ保存する
- 自動投稿の実行器は生成方式を意識せず、承認済み下書きだけを投稿する

## API制約

- 外部検索はキーワードごとに一日一回を基本とする
- 取得結果はFirestoreに保存し、生成のたびに外部APIを呼ばない
- 取得対象はユーザーが設定したキーワードと承認済みアカウントに限定する
- Threadsの外部探索は後回しにし、Xでループを成立させる

## 完成条件

- 主ナビゲーションが「ダッシュボード」「アカウント」「分析」の3本になる
- 参考アカウントを登録し、外部投稿を保存できる
- 外部投稿から抽出したパターンが生成プロンプトに含まれる
- 新仕様で生成した下書きが既存スケジューラーで投稿できる
- 既存の自動投稿を停止せず、アカウント単位で移行できる
- `npm run build` が成功する

## Firestore無料枠での運用要件

- 日次目標を読み取り10,000件以下、書き込み5,000件以下とし、無料枠に十分な余裕を残す
- 投稿案生成のためにアカウントの投稿・下書きを全件取得しない
- 定期同期では新規投稿と直近48時間の指標更新対象だけをFirestoreへ保存する
- スケジューラーは取得済みアカウントを再取得せず、公開失敗時も下書きを失わない
- ダッシュボード初期表示で同じアカウント・投稿・下書きを二重取得しない
- 複合インデックス不足時のフォールバックも取得件数を上限付きにする
- 投稿・下書き・失敗履歴で使う複合インデックスを設定ファイルで管理する
- 投稿対象なのに下書きがない、または投稿・生成に失敗した場合は定期ジョブを失敗終了させる
- 失敗下書きは重複投稿せず、復元可能なアーカイブへ移して稼働キューから除外する
- 上記の差分選択とスケジュール判定を自動テストで検証できる

## 現在地

- [x] 現状の生成プロンプトに既存データを接続
- [x] 外部参考アカウントと探索キーワードのデータモデル
- [x] 外部投稿の収集とキャッシュ
- [x] パターン抽出と新仕様生成
- [x] 主導線の再構成
- [x] 自己改善ループ（型別勝率の集計と生成への反映）
- [x] 既存スケジューラーとの接続確認
- [x] 投稿案生成の全件走査を上限付きクエリへ変更
- [x] 投稿同期を差分・直近指標更新方式へ変更
- [x] スケジューラーの重複取得と失敗時データ消失を解消
- [x] ダッシュボード初期表示の二重取得を解消
- [x] Firestore無料枠向け回帰テストとビルド検証
- [x] スケジューラと自動生成のアカウント単位失敗をActionsへ伝播
- [x] Firestore複合インデックスをコード管理
- [x] 失敗下書きの復元可能なアーカイブ処理
